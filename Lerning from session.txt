@Valid -- if we add this, then it make sure we get some better error messages when we pass the bad data in request and it gives 400 error instead of 500

@BindingResult -- it basically invokes the validator framework on an object being passed in the request


Component we installed
========================
npm install -g create-react-app  ---> using -g, we can install the module globally 
i.e. it will be added in the node_modules folder of base/installation node js folder
npm install boostrap
npm install react-router-dom
npm install classnames i.e. npm i classnames

if we want specifi version of module to be installed then

npm install react-router-dom@4.3.1


Class based component
Functional based component

class based component has life cycle hooks and state which functional based component don't have and those are stateless.


Controlled component
======================


React-Redux architechture
===========================

redux-thunk - to dispatch reducer action to redux store
react-redux - to hookup redux with react

We need to configure redux store to use this along with react application

Go to Redux tool bar and click on instructions and copy below from this url
https://github.com/zalmoxisus/redux-devtools-extension#usage

+  window.__REDUX_DEVTOOLS_EXTENSION__ && window.__REDUX_DEVTOOLS_EXTENSION__()



ES6 syntax
https://www.w3schools.com/react/react_es6.asp

What is JSX?
JSX stands for JavaScript XML.

JSX allows us to write HTML in React.

JSX makes it easier to write and add HTML in React.

JSX is an extension of the JavaScript language based on ES6, and is translated into regular JavaScript at runtime.

https://www.w3schools.com/react/react_jsx.asp



